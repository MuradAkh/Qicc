    int n;
    int m;
    int k = 0;
    int i,j;
    __CPROVER_assume((10 <= n && n <= 10000)) ;
    __CPROVER_assume((10 <= m && m <= 10000));  
    for (i = 0; i < n; i++) {
	for (j = 0; j < m; j++) {
	    k ++;
	}
    }